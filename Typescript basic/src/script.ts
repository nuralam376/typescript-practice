let playerName;

playerName = "Abc";

console.log(playerName);

playerName = 34;

let players = ["Abc", 34, {}];

players.push(34);

players.push({
  name: "Def",
  age: 25,
});

let player = {
  name: "'Ghi",
  age: 25,
  country: "",
};

player.country = "Bd";

function multiply(a: number, b: number) {
  return a + b;
}

console.log(multiply(3, 4));
