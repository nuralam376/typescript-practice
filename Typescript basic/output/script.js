"use strict";
// let playerName;
function drawRectangle(options) {
    let width = options.width;
    let height = options.height;
}
let options = {
    width: 800,
    height: 455,
    length: 877,
};
drawRectangle(options);
